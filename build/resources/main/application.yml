server:
  port: 8081
  servlet:
    context-path: /tchugri
spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/spring
    username: daniel
    password: juvenal123
    driver-class:
      name: org.postgresql.Driver